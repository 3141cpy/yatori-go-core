# 学习通越权漏洞全面评估报告

**审计日期**: 2026-05-26

**审计范围**: 基于密钥泄露的越权漏洞全面评估（不局限于云盘功能）

**测试账号**: 账号1(puid=252798154), 账号2(puid=239448447)

**测试轮次**: 2轮（基础测试24项 + 深入测试11项，共35项）

---

## 一、审计概述

本次评估基于前三个阶段的安全评估发现（个人云盘IDOR、小组云盘IDOR、密钥泄露审计），系统性地分析了泄露密钥对越权漏洞的实际影响，并在云盘之外的功能模块中进行了越权测试。

**核心发现**: 泄露的密钥和签名算法确实降低了越权攻击门槛，但各模块的防护水平参差不齐。部分模块（课程cpi访问、阅读任务）存在严重的越权漏洞，而其他模块（视频学时、人脸验证）已实施了Cookie身份绑定校验。

---

## 二、测试结果汇总

- **总测试数**: 35
- **发现隐患数**: 14
- **严重(CRITICAL)**: 4
- **高危(HIGH)**: 5
- **中危(MEDIUM)**: 1
- **低危(LOW)**: 4

### 按模块统计

| 模块 | 测试数 | 存在隐患数 | 最高风险等级 | 关键发现 |
|---|---|---|---|---|
| 课程信息 | 6 | 3 | HIGH | cpi越权可访问课程页面 |
| 作业系统 | 3 | 0 | LOW | API返回登录页，防护有效 |
| 考试系统 | 3 | 0 | LOW | API返回登录页，防护有效 |
| 人脸验证 | 6 | 2 | CRITICAL | enc签名可构造，但与Cookie绑定 |
| 视频学时 | 5 | 2 | CRITICAL | enc签名可构造，签名校验有效 |
| 用户信息 | 3 | 0 | LOW | enc与Cookie绑定校验 |
| JWT认证 | 2 | 1 | MEDIUM | Payload可解码，签名密钥未破解 |
| 阅读任务 | 4 | 3 | CRITICAL | **签名绕过+跨用户提交均成功** |
| 个人云盘 | 1 | 0 | LOW | Token+puid越权被拒绝 |
| 综合攻击链 | 2 | 2 | CRITICAL | 攻击链部分可行 |

### 按越权类型统计

| 越权类型 | 测试数 | 成功数 | 风险评估 |
|---|---|---|---|
| 水平越权 | 9 | 2 | 课程cpi越权存在风险 |
| 签名绕过越权 | 14 | 5 | 阅读任务签名绕过严重 |
| 水平越权+签名绕过 | 5 | 1 | 阅读任务跨用户提交成功 |
| 基线 | 3 | 2 | 人脸验证/阅读任务基线正常 |
| 签名校验 | 1 | 1 | 视频学时签名校验有效 |
| 信息泄露 | 1 | 1 | JWT Payload信息泄露 |
| 垂直越权 | 1 | 0 | JWT签名密钥未破解 |

---

## 三、核心发现

### 3.1 🔴 最严重：阅读任务签名绕过+跨用户越权（CRITICAL）

**X2-02测试确认**: 使用账号1的Cookie+账号2的userid+泄露的K5盐值构造的enc签名，**成功提交了阅读任务**。

这意味着：
- 攻击者可以为**任意用户**伪造阅读任务完成记录
- 服务端**未校验**签名中的userid与Cookie身份的一致性
- 结合K5盐值泄露，攻击者可批量伪造阅读记录

**攻击步骤**:
1. 获取目标用户的puid和课程信息
2. 使用K5盐值(`NrRzLDpWB2JkeodIVAn4`)构造enc签名
3. 使用自己的Cookie+目标用户的userid+构造的enc签名提交阅读任务
4. 服务端接受请求，目标用户的阅读任务被标记为完成

### 3.2 🔴 课程cpi越权访问（HIGH）

**X1-01测试确认**: 使用账号1的Cookie+账号2的cpi参数，**5/5个课程均可访问**课程页面。

这意味着：
- cpi参数未与用户身份绑定
- 任何已登录用户可通过修改cpi参数访问他人课程页面
- 虽然API级别的章节/任务点接口返回404（可能需要代理），但Web页面级别的课程信息可被越权访问

**限制**: 深入测试显示，API级别的课程章节详情(X1-02)和任务点(X1-03)返回404，可能需要OnlyProxy环境才能访问。

### 3.3 🟡 人脸验证：enc与Cookie身份绑定（部分安全）

深入测试揭示了人脸验证的安全机制：

| 测试 | 结果 | 说明 |
|---|---|---|
| 自己Cookie+自己enc | ✅ 成功 | 基线正常 |
| 自己Cookie+他人enc | ❌ 被拒绝 | "enc验证失败" |
| 无Cookie+自己enc | ❌ 被拒绝 | "非法请求" |

**结论**: 人脸验证API**已实施enc与Cookie身份绑定校验**——enc签名中的puid必须与Cookie中的uid一致。这比个人云盘的授权模型（仅校验Token-puid匹配）更安全。

**但仍有风险**: 如果攻击者同时拥有目标用户的Cookie（如通过XSS），则可使用K4盐值构造合法的enc签名绕过人脸验证。

### 3.4 🟡 视频学时：签名校验有效但API访问受限

| 测试 | 结果 | 说明 |
|---|---|---|
| 自己Cookie+自己userid+正确enc | ❌ 405 | API端点可能需要特定路径参数 |
| 自己Cookie+自己userid+错误enc | ❌ 被拒绝 | 签名校验有效 |
| 自己Cookie+他人userid+正确enc | ❌ 被拒绝 | userid与Cookie不一致 |

**结论**: 视频学时API的签名校验有效，且可能校验了Cookie身份。但405状态码表明API路径可能不正确（需要dtoken等参数），无法完全确认Cookie身份绑定校验。

### 3.5 🟢 作业/考试系统：防护有效

作业和考试系统的API均返回登录页面，表明这些API需要有效的Cookie认证，且可能实施了更严格的访问控制。

### 3.6 🟢 JWT令牌：签名密钥独立且安全

- JWT签名密钥与代码库中其他已知密钥不同
- 19个候选密钥（含常见弱密钥和硬编码密钥）均未通过验证
- 但源码中硬编码的JWT令牌（含完整Cookie）仍为严重信息泄露

---

## 四、密钥泄露对越权漏洞的影响矩阵

| 功能模块 | 密钥泄露影响 | 越权可行性 | 实际测试结果 |
|---|---|---|---|
| 阅读任务 | K5盐值可直接构造enc签名 | ✅ 越权成功 | 跨用户提交阅读任务成功 |
| 课程信息 | cpi参数可被篡改 | ✅ 越权成功 | 跨用户cpi访问课程页面成功 |
| 人脸验证 | K4盐值可构造enc签名 | ⚠️ 部分可行 | enc与Cookie绑定，需同时拥有Cookie |
| 视频学时 | K3盐值可构造enc签名 | ⚠️ 未完全确认 | API返回405，签名校验有效 |
| 个人云盘 | K6 Token+puid可构造请求 | ❌ 越权失败 | Token+puid越权被拒绝 |
| 作业系统 | 需Cookie认证 | ❌ 越权失败 | API返回登录页 |
| 考试系统 | 需Cookie认证 | ❌ 越权失败 | API返回登录页 |
| 用户信息 | K4+K6可构造请求 | ❌ 越权失败 | enc与Cookie绑定 |
| JWT认证 | 密钥未泄露 | ❌ 越权失败 | 19个候选密钥均不匹配 |

---

## 五、已确认的越权漏洞清单

| 编号 | 漏洞名称 | 模块 | 越权类型 | 风险等级 | 状态 |
|---|---|---|---|---|---|
| V1 | 阅读任务跨用户提交 | 阅读任务 | 水平越权+签名绕过 | CRITICAL | ✅ 已确认 |
| V2 | 课程cpi越权访问 | 课程信息 | 水平越权 | HIGH | ✅ 已确认 |
| V3 | 阅读任务签名绕过 | 阅读任务 | 签名绕过越权 | HIGH | ✅ 已确认 |
| V4 | 人脸验证enc签名可构造 | 人脸验证 | 签名绕过越权 | CRITICAL | ⚠️ 需Cookie配合 |
| V5 | 视频学时enc签名可构造 | 视频学时 | 签名绕过越权 | CRITICAL | ⚠️ 未完全确认 |
| V6 | JWT Payload信息泄露 | JWT认证 | 信息泄露 | MEDIUM | ✅ 已确认 |

---

## 六、攻击链分析

### 6.1 阅读任务越权攻击链（已验证可行）

```
1. 获取目标用户的puid（可通过课程列表推断）
2. 获取目标用户所在课程的courseId和classId
3. 使用K5盐值(NrRzLDpWB2JkeodIVAn4)构造enc签名:
   enc = md5(排序参数值 + 'NrRzLDpWB2JkeodIVAn4')
4. 使用自己的Cookie + 目标用户的userid + 构造的enc签名
5. POST /ananas/job/readv2
6. 服务端接受请求 → 目标用户的阅读任务被标记完成
```

**影响**: 攻击者可为任意用户批量完成阅读任务，影响学习数据的真实性。

### 6.2 课程信息越权攻击链（已验证可行）

```
1. 获取目标用户的cpi（可通过课程列表或讨论区推断）
2. 使用自己的Cookie + 目标用户的cpi参数
3. GET /visit/stucoursemiddle?courseid=X&clazzid=X&cpi=目标cpi
4. 服务端返回课程页面 → 可查看课程基本信息
```

**影响**: 可查看他人课程的基本信息，但无法访问章节详情和任务点（API级别返回404）。

### 6.3 人脸验证绕过攻击链（需Cookie配合）

```
1. 通过XSS/会话劫持获取目标用户的Cookie
2. 使用K4盐值(uWwjeEKsri)为目标用户构造enc签名:
   enc = md5(puid + 'uWwjeEKsri')
3. 使用目标用户的Cookie + 构造的enc签名
4. 请求 /passport2-api.chaoxing.com/api/getUserFaceid
5. 服务端接受请求 → 绕过人脸验证
```

**限制**: 需要同时拥有目标用户的Cookie，单独的enc签名无法绕过。

---

## 七、与前阶段评估的对比

| 模块 | 阶段一/二评估 | 本次评估 | 变化 |
|---|---|---|---|
| 个人云盘 | IDOR成功（Token-puid匹配即可） | Token+puid越权被拒绝 | ⚠️ 可能已修复或Token已更换 |
| 小组云盘 | groupweb校验成员身份 | 未重复测试 | - |
| 课程信息 | 未测试 | cpi越权成功 | 🆕 新发现 |
| 阅读任务 | 未测试 | **跨用户提交成功** | 🆕 新发现（最严重） |
| 人脸验证 | enc签名可构造 | enc与Cookie绑定 | ✅ 比预期更安全 |
| 视频学时 | enc签名可构造 | 签名校验有效 | ⚠️ 需进一步确认 |

---

## 八、修复建议

### 8.1 紧急修复（CRITICAL）

1. **阅读任务身份绑定校验**: /ananas/job/readv2 接口必须校验请求中的userid与Cookie/Token中的身份一致性
2. **阅读任务签名服务端化**: K5盐值应迁移到服务端，enc签名由服务端生成
3. **课程cpi参数校验**: 校验cpi与请求者身份的绑定关系，防止跨用户cpi访问
4. **人脸验证签名服务端化**: K4盐值应迁移到服务端，即使当前有Cookie绑定校验

### 8.2 中期加固（HIGH）

5. **视频学时签名服务端化**: K3盐值应迁移到服务端
6. **API统一认证**: 作业/考试等API的认证机制应推广到所有模块
7. **cpi参数动态化**: cpi应包含时效性和身份绑定信息
8. **JWT签名密钥强化**: 确保HS256签名密钥为高强度随机密钥（≥32字节）
9. **清除源码硬编码Cookie**: 移除所有硬编码的Cookie和JWT令牌

### 8.3 长期优化（MEDIUM）

10. **零信任架构**: 不信任任何客户端签名，所有敏感操作需服务端二次验证
11. **行为分析**: 增加服务端行为分析，检测异常请求模式（如批量提交阅读任务）
12. **密钥轮换**: 建立定期密钥轮换机制
13. **请求频率限制**: 对学时提交、人脸验证、阅读任务等敏感操作增加频率限制

---

## 九、各模块详细测试结果

### 9.1 课程信息

| 测试ID | 测试名称 | 结果 | 说明 |
|---|---|---|---|
| A1-01 | 跨用户cpi访问课程详情 | 🔴 越权成功 | 使用账号1的Cookie+账号2的cpi成功获取课程数据 |
| A1-02 | 泄露Token构造移动端请求 | 🟢 安全 | Token+DES密钥构造的请求未能获取课程列表 |
| A1-03 | Cookie身份校验测试 | 🟢 安全 | 不同Cookie获取的课程列表不同 |
| X1-01 | 跨用户cpi批量访问课程 | 🔴 越权成功 | 5/5个课程均可访问 |
| X1-02 | 跨用户cpi访问课程章节详情 | 🟢 安全 | API返回404 |
| X1-03 | 跨用户cpi访问课程任务点 | 🟢 安全 | API返回404 |

### 9.2 作业系统

| 测试ID | 测试名称 | 结果 | 说明 |
|---|---|---|---|
| B1-01 | 正常访问作业列表 | 🟢 安全 | API返回登录页 |
| B1-02 | 跨用户cpi访问他人作业 | 🟢 安全 | 被拒绝 |
| B1-03 | 泄露Token访问作业列表 | 🟢 安全 | 失败 |

### 9.3 考试系统

| 测试ID | 测试名称 | 结果 | 说明 |
|---|---|---|---|
| C1-01 | 正常访问考试列表 | 🟢 安全 | API返回登录页 |
| C1-02 | 跨用户cpi访问他人考试 | 🟢 安全 | 被拒绝 |
| C1-03 | 泄露Token访问考试列表 | 🟢 安全 | 失败 |

### 9.4 人脸验证

| 测试ID | 测试名称 | 结果 | 说明 |
|---|---|---|---|
| D1-01 | 为目标用户构造人脸enc签名 | 🟡 风险 | 签名可构造 |
| D1-02 | 使用他人enc签名请求人脸验证API | 🟢 安全 | enc与Cookie绑定，"enc验证失败" |
| D1-03 | 人脸验证状态接口测试 | 🟢 安全 | "无权限" |
| X4-01 | 自己Cookie+自己enc请求 | 🟢 基线 | 成功 |
| X4-02 | 自己Cookie+他人enc请求 | 🟢 安全 | "enc验证失败" |
| X4-03 | 无Cookie+自己enc请求 | 🟢 安全 | "非法请求" |

### 9.5 视频学时

| 测试ID | 测试名称 | 结果 | 说明 |
|---|---|---|---|
| E1-01 | 为目标用户构造视频学时enc签名 | 🟡 风险 | 签名可构造 |
| E1-02 | 使用他人userid+enc提交学时 | 🟢 安全 | 被拒绝(405) |
| E1-03 | 泄露Token构造移动端视频学时请求 | 🟢 安全 | 失败(405) |
| X3-01 | 自己Cookie+自己userid+enc | 🟡 不确定 | 405（API路径问题） |
| X3-02 | 使用错误enc提交学时 | 🟢 安全 | 签名校验有效，被拒绝 |

### 9.6 用户信息

| 测试ID | 测试名称 | 结果 | 说明 |
|---|---|---|---|
| F1-01 | 跨用户请求人脸信息API | 🟢 安全 | "enc验证失败" |
| F1-02 | 泄露Token访问用户信息 | 🟢 安全 | "非法请求" |
| F1-03 | i.chaoxing.com用户设置页面 | 🟢 安全 | 302重定向 |

### 9.7 JWT认证

| 测试ID | 测试名称 | 结果 | 说明 |
|---|---|---|---|
| G1-01 | JWT令牌Payload解码 | 🟡 风险 | Payload可解码，含uid/loginTime/exp |
| G1-02 | JWT签名密钥暴力破解 | 🟢 安全 | 19个候选密钥均不匹配 |

### 9.8 阅读任务

| 测试ID | 测试名称 | 结果 | 说明 |
|---|---|---|---|
| H1-01 | 为目标用户构造阅读任务签名 | 🔴 风险 | 签名可构造 |
| H1-02 | 使用泄露签名提交阅读任务 | 🔴 越权成功 | 签名绕过成功 |
| X2-01 | 使用泄露签名提交自己的阅读任务 | 🔴 越权成功 | 签名绕过成功 |
| X2-02 | 使用泄露签名+目标用户userid | 🔴 **越权成功** | **跨用户提交阅读任务成功** |

### 9.9 个人云盘

| 测试ID | 测试名称 | 结果 | 说明 |
|---|---|---|---|
| X5-01 | 泄露Token+目标用户puid访问云盘 | 🟢 安全 | 被拒绝 |
